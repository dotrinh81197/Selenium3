# Load pages faster without waiting for all resources (e.g. images, fonts)
selenide.pageLoadStrategy=eager

# Set the browser window size (works only if not overridden in code)
selenide.browserSize=1920x1080